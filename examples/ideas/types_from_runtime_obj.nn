
# Creates an interface based on a class.
const Ta = Like<SomeClass>;

# Sources the type of a predefined value.
const a = 3;
const Tb = typeof a;

# Sources the keys of a predefined object, struct or class.
const paul_obj = {
    name: "Paul"
    age: 10
}

const Tc = keyof paul_obj  # Union ["name", "age"]